/**
 * @property {string} img - The URL of the project's image.
 * @property {string} alt - The alt text for the project's image.
 * @property {string} title - The title of the project.
 * @property {string} description - A brief description of the project.
 * @property {string} link - The URL to the project's page.
 */
export interface Project {
  img: string;
  alt: string;
  title: string;
  description: string;
  link: string;
}

